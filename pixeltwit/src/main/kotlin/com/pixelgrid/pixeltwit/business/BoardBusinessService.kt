package kotlin.com.pixelgrid.pixeltwit.business

import com.pixelgrid.pixeltwit.model.Board
import com.pixelgrid.pixeltwit.model.Cell

public class BoardBusinessService{
    public int createImage(Board b){
        //This is where you can take apart board and put that shit into the thing ian is doing and make an image. Then we can create another method to ship that bitch
        //if(worked){return true}
        //else{return false}
    }
    public boolean tweetBoard(){
        //Ship that bitch
        //if(worked){return true}
        //else{return false}
    }
}